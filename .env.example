# Copy this file to .env and fill in real values for your environment.
# Expo inlines any EXPO_PUBLIC_* variable into the JS bundle at build time.

# Base URL of the Stray Paws API (e.g. https://api.straypaws.app).
# Leave empty to run the app in offline/demo mode against local mock data.
EXPO_PUBLIC_API_BASE_URL=
